/**
 * Centralized branding constants for the backend.
 *
 * Change values here to rebrand emails, notifications, and any server-side
 * user-facing text in one place.
 */

export const APP_NAME = "GatePass+";
export const APP_TAGLINE = "Society Management Platform";

/** Default "From" address when EMAIL_FROM env var is not set. */
export const DEFAULT_EMAIL_FROM = `${APP_NAME} <noreply@gatepass.app>`;
